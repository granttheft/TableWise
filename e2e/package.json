{
  "name": "tablewise-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:admin": "playwright test --project=admin-panel",
    "test:booking": "playwright test --project=booking-ui",
    "test:super-admin": "playwright test --project=super-admin",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0"
  }
}
